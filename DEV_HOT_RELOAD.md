# Nightingale 热部署开发指南

## 什么是热部署？

热部署（Hot Reload）允许您在修改代码后自动重新编译和重启应用程序，无需手动重启，大大提高开发效率。

## 安装 air 工具

如果还没有安装 air，请运行：

```powershell
go install github.com/air-verse/air@latest
```

## 使用方法

### 方法一：使用启动脚本（推荐）

```powershell
.\dev.ps1
```

### 方法二：直接使用 air 命令

```powershell
air
```

### 方法三：使用 Makefile（如果支持）

```bash
make dev
```

## 配置说明

热部署配置文件：`.air.toml`

主要配置项：
- `build.cmd`: 构建命令
- `build.bin`: 生成的二进制文件路径
- `exclude_dir`: 忽略的目录
- `include_ext`: 监听的文件扩展名

## 工作流程

1. 启动热部署后，air 会监听项目中的 `.go` 文件变化
2. 当您修改代码并保存时，air 会自动：
   - 重新编译项目
   - 停止旧进程
   - 启动新进程
3. 如果编译出错，会在控制台显示错误信息
4. 应用程序会自动重启并加载最新代码

## 注意事项

1. **首次启动**：第一次启动可能需要较长时间进行编译
2. **文件监听**：air 会监听所有 `.go` 文件的变化
3. **临时文件**：编译后的二进制文件会保存在 `tmp/` 目录
4. **端口冲突**：确保应用程序使用的端口没有被其他进程占用
5. **数据库连接**：热部署不会影响数据库连接，但建议在开发环境中使用独立的数据库

## 停止服务

按 `Ctrl+C` 停止热部署服务。

## 故障排除

### 1. air 命令未找到
```powershell
# 检查 GOPATH 是否在 PATH 中
echo $env:GOPATH
# 如果不在，添加到 PATH
$env:PATH += ";$env:GOPATH\bin"
```

### 2. 编译错误
- 查看控制台输出的错误信息
- 检查 `tmp/build-errors.log` 文件
- 确保所有依赖都已正确安装

### 3. 端口被占用
```powershell
# 查看端口占用情况
netstat -ano | findstr :8080
# 结束占用进程
taskkill /PID <进程ID> /F
```

## 性能优化

1. **排除不必要的目录**：在 `.air.toml` 中的 `exclude_dir` 添加不需要监听的目录
2. **调整监听间隔**：修改 `delay` 参数（默认 1000ms）
3. **使用 SSD**：提高文件系统性能

## 开发建议

1. **小步快跑**：频繁保存文件，及时看到修改效果
2. **错误处理**：注意控制台输出的编译错误
3. **日志监控**：关注应用程序的日志输出
4. **测试验证**：每次修改后及时测试功能
