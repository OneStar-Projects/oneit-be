[Unit]
Description=Categraf Agent
After=network.target

[Service]
Type=simple
User={{ user }}
Group={{ group }}
ExecStart={{ install_dir }}/categraf --config={{ config_dir }}/config.toml
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target